import type { WebTransportBundle } from "./transport/types";
import type { BrowserCapabilities, InputStats, PocketInstallStatus, VoiceSdkCapabilities, VoiceEvent, VoiceRuntimeConfig, VoiceRecord } from "./types";
type EventHandler = (event: VoiceEvent) => void;
export declare class VoiceCoreWeb {
    private static readonly OUTPUT_POLL_IDLE_MS;
    private static readonly OUTPUT_POLL_ACTIVE_MS;
    private static readonly STREAM_TEXT_FLUSH_INTERVAL_MS;
    private static readonly MAX_OUTPUT_SECONDS_PER_DRAIN;
    private static readonly MAX_OUTPUT_CHUNKS_PER_DRAIN;
    private static readonly POST_SPEECH_STT_COOLDOWN_MS;
    private static readonly AEC_POST_SPEECH_STT_COOLDOWN_MS;
    private static readonly BARGE_IN_COOLDOWN_MS;
    private static readonly BARGE_IN_ECHO_CLEAN_THRESHOLD;
    private static readonly BARGE_IN_NO_AEC_THRESHOLD;
    private static readonly EMPTY_LLM_RETRY_MAX_OUTPUT_TOKENS;
    private readonly audioGraph;
    private readonly runtimeWorker;
    private readonly handlers;
    private config;
    private capabilities;
    private state;
    private runtimeStatus;
    private legacyState;
    private running;
    private drainingAudio;
    private outputDrainGeneration;
    private outputPlaybackActive;
    private outputBackpressured;
    private drainTimerId;
    private transports;
    private sttUnsubscribe;
    private sttErrorUnsubscribe;
    private readonly sttSendQueue;
    private sttLifecycleChain;
    private sttLifecycleGeneration;
    private sttSendGeneration;
    private sttTransportRunning;
    private sttTransportOwned;
    private sttPausedForAssistant;
    private sttRestartTimerId;
    private responseAbortController;
    private responseGeneration;
    private lastAssistantPreviewText;
    private activeTurnStartedAtMs;
    private userSpeechActive;
    private bargeInFrameCount;
    private bargeInTriggered;
    private bargeInGraceUntilMs;
    private bargeInLastTimeMs;
    private runtimeFailureActive;
    private readonly lifecycleQueue;
    private destroyed;
    private cleanupRequired;
    private startupGeneration;
    private startupFailure;
    constructor(config?: VoiceRuntimeConfig);
    start(): Promise<void>;
    private startInternal;
    stop(): Promise<void>;
    private assertStartupCurrent;
    private stopInternal;
    destroy(): Promise<void>;
    isRunning(): boolean;
    setConfig(config: Partial<VoiceRuntimeConfig>): Promise<void>;
    private assertCommercialAccess;
    setTransports(transports: WebTransportBundle): Promise<void>;
    private setTransportsInternal;
    pushPartialTranscript(text: string): Promise<void>;
    completeUserTranscript(text: string): Promise<void>;
    pushAssistantText(text: string, isFinal?: boolean): Promise<void>;
    pushTtsAudio(samples: Int16Array | number[], sampleRateHz: number, channels: number, timestampMs?: number): Promise<void>;
    private pushTtsAudioSlices;
    getContextJson(): Promise<string>;
    getInputStats(): Promise<InputStats>;
    getCapabilities(): BrowserCapabilities | null;
    getSdkCapabilities(): VoiceSdkCapabilities;
    getPocketInstallStatus(): Promise<PocketInstallStatus>;
    installPocketModel(): Promise<PocketInstallStatus>;
    cancelPocketModelInstall(): Promise<PocketInstallStatus>;
    removePocketModel(): Promise<void>;
    listVoices(): Promise<VoiceRecord[]>;
    activateVoice(_voiceId: string): Promise<void>;
    onEvent(handler: EventHandler): () => void;
    private emit;
    private emitNormalizedEvent;
    private setRuntimeStatus;
    private drainOutputAudio;
    private handleInputFrames;
    private scheduleDrainOutputAudio;
    private clearDrainTimer;
    private failRuntime;
    private enqueueLifecycle;
    private handleOutputPlaybackState;
    private handleOutputQueuePressure;
    private startTransports;
    private stopTransports;
    private handleSttTranscript;
    private abortResponsePipeline;
    private syncSttTransportForState;
    private queueSttLifecycle;
    private clearSttRestartTimer;
    private getPostSpeechSttCooldownMs;
    private syncBargeInWindowForState;
    private markUserSpeechStarted;
    private detectLocalBargeIn;
    private executeLocalBargeIn;
    private resetBargeInDetector;
    private generateAssistantResponse;
    private float32ToPcm16;
    private streamAssistantReply;
    private static readonly MIN_INCREMENTAL_TTS_CHUNK_CHARS;
    private static readonly CLAUSE_FALLBACK_CHARS;
    private extractSpeakableText;
    private findLastClauseBoundary;
    private findFirstSpeakableBoundary;
    private synthesizeAssistantChunk;
    private emitLatencyDebug;
    private prepareTextForTts;
    private enqueueSttSend;
}
export {};
//# sourceMappingURL=VoiceCoreWeb.d.ts.map