# T06: Canonical acceptance, documentation and landing

Revision r1; phase P3; detail **outlined**. Graph owns dependencies/status. Requirements: R06, R09, R12; acceptance IDs are the corresponding A numbers in specification.md. Owner: implementation lead; independent review: available separate reviewer. Baseline f25bbf48c must be refreshed before implementation.

Owned resources (proposed new paths are not current-code claims):
- `docs/creator-command-center/`
- `environment/canonical-artist-os`

Read [architecture](../architecture.md), [specification](../specification.md), and [readiness](../readiness.md). You are not alone in the repository: preserve other agents' edits; coordinate shared types with the lead. No blanket staging, stash, app restart, new provider purchase, or real external write is authorized by this packet.

## Work

Expand live procedure after P2. In a designated disposable profile, open Profile, set explicit artist anchors, enrich through real configured retrieval, inspect sources, reload, correct/remove, refresh and ask Manager a career-specific question. Confirm answer cites surviving facts, respects artist direction and can independently retrieve existing analytics without launching capture. Verify no-login/no-Spotify artist path, partial research and keyboard layout. Record build/commit/profile identity; no app restart without standing-rule authorization. Update spec implementation status honestly, attach independent review and actual test evidence, commit exact owned paths, catch up with main, reverify and land/push preserving others' unfinished files. Do not mark live acceptance complete from fixtures.

## Validation and handoff

From the verified own Artist OS worktree, run new/existing targeted tests using `PANGOCAIRO_BACKEND=fontconfig bun test ./<verified-test-path> --path-ignore-patterns='**/release-artist-os/**' --path-ignore-patterns='**/dist/**'`. Replace the path with the actual tests created/selected above and record it; isolated built-in mocks run alone. For P1 new service tests, use proposed `./packages/server-core/src/artist-profile-enrichment/service.test.ts` and shared `./packages/shared/src/artist-context/career-research.test.ts` once created. Also run `bun run typecheck:all`, `bun run check:dependency-containment`, and `git diff --check` for the integrated phase. Documentation-only edits do not need an app test suite.

Expected outcome: the task's associated A checks prove persisted user-visible behavior, not just existence of components. Evidence goes in this packet's evidence directory with actual revision, commands, outcomes and mock/live distinction. Return changed paths, commit, failures/limits and interface changes. Stop and resolve a source-contract or permission mismatch; do not bypass by expanding agent capabilities. Rollback only this feature's changes and preserve data. Later outlined tasks must be expanded before starting.
