# T02: Deliver source-to-profile-to-Manager thin slice

Revision r1; phase P1; detail **executable**. Graph owns dependencies/status. Requirements: R01, R02, R03, R04, R05, R06, R08, R09, R10, R12; acceptance IDs are the corresponding A numbers in specification.md. Owner: implementation lead; independent review: available separate reviewer. Baseline f25bbf48c must be refreshed before implementation.

Owned resources (proposed new paths are not current-code claims):
- `packages/server-core/src/artist-profile-enrichment/`
- `packages/server-core/src/deep-research/DeepResearchRunner.ts`
- `packages/server-core/src/handlers/rpc/artist-profile-enrichment.ts`
- `packages/shared/src/protocol/channels.ts`
- `apps/electron/src/renderer/components/app-shell/ArtistHQHome.tsx`
- `packages/server-core/src/hq-state/manager-tools.ts`

Read [architecture](../architecture.md), [specification](../specification.md), and [readiness](../readiness.md). You are not alone in the repository: preserve other agents' edits; coordinate shared types with the lead. No blanket staging, stash, app restart, new provider purchase, or real external write is authorized by this packet.

## Work

Read DeepResearchRunner start/execute/cancel and existing source profiles, RPC registration/transport, Profile draft save, and manager-tools get_artist_context. Add optional enrichment-purpose owner metadata compatibly; create the narrow host adapter and RPC registration. START must persist binding and run id before returning; completion validates structured facts rather than scraping the report's prose into a profile. Connect real source receipts and enforce selected tool/deadline budgets. Use planPolicy auto and read-only retrieval; don't alter generic Deep Research approval defaults. Add minimal separate panel below form with Enrich, sources, progress, Cancel and sourced saved facts. Handle dirty form with explicit Save and enrich/Use saved profile inline. Fix draft hydration to depend on the actual Profile body/revision rather than parsed object identity: edit Bio during research, receive progress/publication context broadcasts, and prove the exact unsaved draft survives. Genuine external Profile edits retain the local draft with conflict messaging. Add narrow Manager career retrieval so the initial end-to-end slice is already useful. Demonstrate public fixture and one real source path through fetched evidence, persisted finding, reload and Manager retrieval. Instrument zero Spotify analytics calls/writes; a logged-out Spotify browser must not affect success. Source setup proof is independent of implementation; end-to-end proof belongs to this task.

## Validation and handoff

From the verified own Artist OS worktree, run new/existing targeted tests using `PANGOCAIRO_BACKEND=fontconfig bun test ./<verified-test-path> --path-ignore-patterns='**/release-artist-os/**' --path-ignore-patterns='**/dist/**'`. Replace the path with the actual tests created/selected above and record it; isolated built-in mocks run alone. For P1 new service tests, use proposed `./packages/server-core/src/artist-profile-enrichment/service.test.ts` and shared `./packages/shared/src/artist-context/career-research.test.ts` once created. Also run `bun run typecheck:all`, `bun run check:dependency-containment`, and `git diff --check` for the integrated phase. Documentation-only edits do not need an app test suite.

Expected outcome: the task's associated A checks prove persisted user-visible behavior, not just existence of components. Evidence goes in this packet's evidence directory with actual revision, commands, outcomes and mock/live distinction. Return changed paths, commit, failures/limits and interface changes. Stop and resolve a source-contract or permission mismatch; do not bypass by expanding agent capabilities. Rollback only this feature's changes and preserve data. Later outlined tasks must be expanded before starting.
