# T-01 — Locate and freeze the execution boundary contract

Revision r2 · Phase P-01 · Detail executable · Owner runtime owner · Reviewer independent verifier.
Status/dependencies: plan.json (G-01, S-LOCAL). Requirements R-01/R-03/R-04/R-06/R-07/R-10/R-15. Acceptance A-01/04/05/08/09/13.

Mapped acceptance IDs indicate design/prototype coverage in this phase, not full release certification. T-01 proves the baseline and boundary contract; T-02/T-03 prove only their explicitly listed disposable prototype cases. Production A-01–A-20 evidence remains outstanding.

## Ownership and context

Own the P-01 boundary inventory/ADR drafts and disposable test harness directory `packages/server-core/src/workflows/__tests__/durability/` (proposed). This task does not change production dispatch. Other agents may be working in this repository; preserve their edits and report overlaps to the lead.

Baseline is the canonical Artist OS main checkout at 8ce9f5c11 when the plan was authored. Re-read AGENTS.md, verify current branch/worktree/status and compare relevant source changes since that baseline. Do not reset or stash other work.

Read specification.md sections 2, 5–10; runner.ts `rerunFromStep` and `runStep`; run-storage.ts; SessionManager.ts runner construction and actual delegation implementation; shared agent escalation/messaging files; current model bridge chosen for the thin slice. Use targeted searches to identify SDK callbacks; do not assume the turn boundary exists merely because a transcript event is emitted.

## Outcome

Produce `evidence/T-01-boundaries.md` identifying exact source symbols and data ownership for model response, tool preparation, dispatch, result persistence, next-turn launch, approval wait, child launch/join and ScheduledWorkRunner admission. Enumerate paths bypassing those seams. Select one supported model route and one representative workflow/child fixture for the proof; uninstrumentable paths remain explicitly unsupported.

Define the proposed TypeScript adapter surface (normalization, effect policy, operation identity, execute, reconcile, result validation), serialized continuation schema and compatibility manifest. Map them onto current cancellation, permissions and output barriers. Do not redesign the agent prompt format.

## Steps

1. Re-anchor and inventory actual paths, including retries within SDK layers and Python DBOS call sites. Label current behavior separately from planned instrumentation.
2. Specify exact event ordering for complete model turn → tool batch → results → next turn. Prove where a result is committed versus merely displayed.
3. Define a fake provider process with its own durable operation/effect ledger; test controller and app worker must be separate processes. No real network account/credentials.
4. Draft a minimal fixture: model emits an approval-requiring operation, child runs two reads, fake effect can accept then drop reply. Determine injection hooks for each commit/dispatch boundary.
5. Record proposed source ownership for T-02/T-03 and the supported packaged Electron SQLite binding candidate. Identify dependencies already installed; no unrelated dependency upgrades.

## Validation

From `/Users/michaelb.williams/RunnerOS/.worktrees/main/artist-os`, rerun the relevant existing baseline with repository ignore flags:

```bash
PANGOCAIRO_BACKEND=fontconfig bun test \
  --path-ignore-patterns='**/release-artist-os/**' \
  --path-ignore-patterns='**/dist/**' \
  packages/server-core/src/workflows/runner.test.ts \
  packages/shared/src/workflows/storage.test.ts \
  packages/shared/src/automations/scheduler-catch-up.test.ts \
  packages/shared/src/automations/scheduler-state.test.ts \
  packages/shared/src/automations/automation-system.test.ts \
  packages/server-core/src/scheduled-work/ScheduledWorkRunner.test.ts
```

Expected: zero failures. Report fresh counts, do not copy 248 into a new evidence record. Independently inspect each boundary against actual source; an inventory without a proven model/tool boundary does not satisfy R-04.

Evidence includes source SHA/diff digest, current symbols, actual commands/results, fixture protocol and explicit unsupported paths. Do not log credentials or personal transcripts.

## Handoff and stop conditions

T-02 receives the storage/runtime contract; T-03 receives continuation and fault injection contracts. Stop expansion of a route that cannot expose the required boundaries; propose a bounded adapter or restrict the initial route, rather than silently claiming support. Rollback is removal of this task's disposable fixtures only. Return changed paths, baseline tests, unresolved seams and reviewer verdict. Production remains unchanged.

## r2 foundation constraints

Authorized by the user on 2026-09-09 for this first task group, followed by rival review and fixes. The prototype is synthetic-only and cannot admit product work. Resolve replay-payload protection and key-loss behavior now; no plaintext credential/user-content fixtures. Budget/attempt/deadline accounting must precede simulated paid work, durable identity precedes children, and the certification manifest is checked before every dispatch. Never fall back to legacy after admission. DBOS comparison is limited to embedded prerequisites first; Postgres disqualifies the documented TypeScript path without installing it.

## Implementation record

P-01 isolated proof accepted on 2026-09-09. See [evidence](../evidence/T-01-boundaries.md) and [independent review/fixes](../evidence/rival-review.md). Mapped acceptance IDs remain prototype traceability, not complete production certification.
