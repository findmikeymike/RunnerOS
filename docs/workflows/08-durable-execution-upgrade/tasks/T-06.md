# T-06 — Production approval and lifecycle integration

Revision r10. Owner: runtime-owner. Dependencies: G-03 and T-06A through T-06F.

Connect the existing durable read helper slices through the optional Electron host. The default remains off; public workflow START remains legacy until rollout.

- Bind each tool approval to the current local owner, workspace, exact connection/model/credential, validated on-disk policy revision and bounded expiry. Recheck after asynchronous resolution; malformed policy fails closed.
- Revalidate access and frozen binding before every model reservation, including continuation after a completed tool. Persist pause before refusing revoked access.
- Make prolonged quit/update cleanup visible without closing storage while work remains in flight. Failed cleanup permits retry; repeated quit explains the wait.
- Verify the real journal and read runner with native file reads, policy changes, approval replay and late access revocation. Independently review startup/authority and shutdown wiring.

Owned boundaries: server-core durable-read-authorization.ts and durable-read-runner.ts; Electron durable-workflow-authority.ts, durable-workflow-startup.ts, shutdown-wait.ts and lifecycle wiring in index.ts. Preserve existing legacy execution.

Validation: focused Bun tests with repository ignore patterns, shared/server-core/Electron typechecks, isolated updater suite. A copied disposable Electron probe exercises actual safeStorage only if the runtime reaches readiness; a timeout is a blocked proof, never a pass. Do not restart the user's running app without permission.

Completion requires the integration proof and independent review. Public recovery UX and staged activation remain later phases.
