# T-07 — durable effect operations

Revision r10 · executable internal integration · P-03 remains open.

Extend the existing encrypted journal with immutable operation intent keyed by run/workspace/slot. Pin adapter/version, exact normalized input digest, account/credential identity, effect classification, idempotency key, output schema and bounded attempt/cost policy. Persist each dispatch attempt and shared budget reservation before calling an adapter. Never hold a SQLite transaction across external work.

Unknown or in-flight outcomes require authoritative reconciliation before another attempt. Known-not-applied observations alone permit retry under the original identity and remaining limits. Preserve unknown cost reservations. Completed output must pass the pinned schema validator. Cancellation/pause/deadline/ownership fences block new dispatch while authenticated late observations may be recorded without advancing a cancelled run.

Journal owner: shared durable-execution index/types/tests. Integration owner: new server-core effect coordinator and local immutable-artifact adapter. No generic writable Pi-tool allowance; native/model replay support stays explicitly certified.

Proof: real SQLite close/reopen, separate-process interruption after external acceptance before result recording, no second external effect, duplicate dispatch receipts, cancellation with late outcome, changed inputs/account/schema refusal, budget exhaustion, stale owner refusal. Native artifact publication uses a disposable private directory with exclusive identity, content digest and fsync. It is a real local effect, not remote-provider certification. T-07-LIVE remote test remains separately gated by a concrete prepared target/authorization.
