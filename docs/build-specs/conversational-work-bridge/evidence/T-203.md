# T203 live setup — 2026-09-13, not accepted

User authorized opening candidate. PID74321 launched using existing Electron binary; UI visibly loaded conversational-work-spec/apps/electron/dist/renderer/index.html with existing artist profile. Main/preload/T204 source+renderer hashes match. No microphone call or specialist draft started.

Missing worktree Moonshine resources found at first preparation. Added local apps/electron/.voice-core-dev symlink to existing /Users/michaelb.williams/RunnerOS/.worktrees/main/artist-os/apps/electron/.voice-core-dev. Refresh and reprepare succeeded; Moonshine Balanced ready in UI and prepare-ready in log. No download/provider-setting change.

Live blocker: saved Conversation route is OpenRouter, Google Gemini Flash Latest, reasoning Low. T201 native work capability is enabled only for the tested official DeepSeek v4 Flash route with thinking off; current call would retain Command fallback, not native draft admission. Conversation settings left open for review. Need explicit user choice before modifying saved voice route. No voice/worker/saved-result smoke certified.

## Direct provider setup follow-up

Michael connected direct DeepSeek for this test and had authorized switching the conversation route. UI verified DeepSeek V4 Flash on DeepSeek / Runner Backend (API Key) 3; changed Conversation reasoning from Low to Off and observed saved settings. Reopened HQ manager preparation: Moonshine Balanced ready, Start call enabled, and Ready when you are. Left microphone inactive for Michael. The earlier route-selection blocker is resolved; real provider dispatch, background drafting, saved output announcement and uninterrupted call remain unverified. T203 and live exit gates remain pending.

## First live attempt: confirmation loop

At 19:22–19:23 UTC the direct DeepSeek / thinking-off call produced three offers; the two intervening replies logged confirmation=false. Michael reported saying “confirm”. Code inspection found the exact affirmative allowlist included “confirmed” but omitted “confirm”, and native offers bypass contextual intent classification. Added explicit confirm variants; service regressions verify one admission, no Command handoff, no duplicate launch on repeated confirmation, and no admission for negated/conditional/question variants. Targeted suite: 30 pass, 0 fail, 150 assertions. This fixes the explicit phrase omission; broader natural-language native confirmation remains limited. Running process requires the updated main bundle to be loaded before live retest. T203 remains unaccepted.

## Command/background routing correction

Michael reported the old Command offer during the follow-up call. Inspection found the renderer prompt unconditionally said work happens in Command and confirmation only opens an unsent draft, contradicting native draft capability. Replaced those statements with capability-aware instructions; native mode now defaults local draft requests to background work, retains explicit direct-chat and unsupported-work Command routes, and asks for clarification when needed. Command tool description reinforces the distinction when native drafting is available. Offer diagnostics now include background-draft versus command without recording user content.

90 targeted tests pass (521 assertions), including native-enabled Command confirmation emitting handoff_ready without launching work and background confirmation launching without handoff_ready. Existing prompt budget and HQ/campaign context preservation tests pass. Electron type check passes. These are mocked-provider behavioral and prompt-contract tests, not evidence of the real model selecting the intended route. Live routing and saved-result milestone remain pending after app restart. The prior follow-up still used original PID74321, so it did not load the confirmation correction.
