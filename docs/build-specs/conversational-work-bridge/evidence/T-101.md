# T-101 — host admission proof, 2026-09-12

PASS for bounded P1 prototype. Independent reviewer /root/host_map reran 18 tests, 0 failures, 87 assertions (exit 0). Initial review found cancellation after continuation ignored and late questions reopening cancellation; both fixed and regression-covered. Reply IDs now reject different text digests.

Command: PANGOCAIRO_BACKEND=fontconfig bun test packages/server-core/src/voice-tasks/admission-proof.test.ts --path-ignore-patterns='**/release-artist-os/**' --path-ignore-patterns='**/dist/**'. Baseline AgentMessageService suite: 17 pass, 0 fail, 84 assertions.

Real filesystem/atomic receipt storage; fake child creation/execution and host authority. Six failure cuts: persisted reservation, launch intent, receipt write, child creation, execution, bridge write. Child launch correlation recovers the orphan-identity gap. No uncertain attempt relaunches. Same-host concurrent instances share a workspace lock. Intent lookup excludes ephemeral call/focus IDs. Foreign parent/workspace and different payload reject. Two active tasks enforced. Question continuation uses new execution generation in same child; Promise resolution cannot establish success. Cancellation keeps terminal outcomes monotonic.

Permission proof models the proposed host cap, including fail-closed unsupported dispatch and rejection of broad/voice grants. It does NOT exercise production pre-tool policy or certify it. Full continuation receipts and reply transport remain T-301. This is a prototype under voice-tasks only, not a shipping service.

Baseline d0b5bae3c; production dispatch untouched. No providers, microphone, app launch, real workspace writes or Git mutations. Rollback removes only owned proof files.
