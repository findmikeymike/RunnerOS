# T-203 — Real draft and concurrent spoken call

Revision r2; P2; executable after G-P2, T202, S-LIVE and S-APP. Owner implementer; independent reviewer required. Requirements R-01/02/03/04/05/13. No launch permission yet. No commit, merge or push.

## Setup gates

1. Obtain Michael's explicit permission to relaunch the isolated candidate. Preserve existing artist profile and independent voice settings; do not change providers or worker modes silently. The candidate lacks its own Electron binary because dependency lifecycle scripts were disabled; an existing binary is available at /Users/michaelb.williams/RunnerOS/node_modules/electron/dist/Electron.app/Contents/MacOS/Electron. Using that binary to open this worktree does not promote this checkout to canonical. Recheck binary availability before launch.
2. Recheck branch codex/conversational-work-spec, HEAD d0b5bae3c, dirty files and T202 final build hashes. Candidate root is /Users/michaelb.williams/RunnerOS/.worktrees/conversational-work-spec. Main, renderer, preload and resource copies are compiled. No current app process was stopped or restarted during implementation.
3. Launch only after approval with existing development access/profile and CRAFT_ARTIST_VOICE_WORK_BRIDGE=1. Do not automatically start the microphone. Confirm current voice model route supports native work (tested official DeepSeek v4 Flash, thinking off), existing speech provider readiness and one active drafting specialist with its explicit focus. Missing setup blocks S-LIVE; do not fabricate readiness or expose credentials.
4. Record S-APP authorization and fresh setup evidence; these are not accepted merely because compilation passed.

## First milestone procedure

- Michael presses Call. Verify normal opening, input capture, avatar and ordinary response.
- Request one bounded local draft from an existing specialist, naming the appropriate focus (for example Scriptwriter short-form). Confirm the single proposal. Record its existing intent/task/attempt/receipt/child identifiers without sensitive content.
- While that real task runs, speak an unrelated question. Verify a real answer and that the call stays open. Do not simulate delay/results or invoke completeUserTranscript with worker text.
- Verify the worker produced a persisted, nonempty output through its normal output tool. Wait for a quiet moment; hear the actual result announcement with normal lip sync. Verify correlated delivered acknowledgement and Open result points to that saved artifact. A log message or queued TTS is insufficient.
- Separately interrupt an announcement and end/reconnect the call. Verify worker execution is not cancelled by speech control, unresolved requests retain identity, and stale/other-workspace results do not speak. Do not automatically retry uncertain execution.

Record evidence/T-203.md with build identity, observed real/provider/device results versus fixture evidence, exact failures and remaining smoke steps. T203 acceptance and G-P2-EXIT require independent review of this evidence. If live proof fails, fix only the evidenced issue, rebuild and retest before advancing. P3 question/approval continuation remains gated after P2 exit; no crash auto-resume or general sensitive-action capability is promised.

## r3 candidate additions

Smoke the T204 controls on the final build: expand task details by keyboard, request Cancel task during real worker execution while continuing speech, verify pending cancellation becomes a host-confirmed terminal state, and verify a task that completed first keeps its saved output. Repeated clicks must not target another attempt. Static markup tests do not certify actual focus or visual behavior.
