# T-101 — Host admission and lifecycle contract proof

Revision r1; phase P1; owner implementer; reviewer independent-reviewer. Planned, executable after G-P1. Requirements R-02/R-05/R-06/R-07/R-08/R-09/R-11/R-12. Own proposed `packages/server-core/src/voice-tasks/` fixture/prototype and contract amendments only. Do not edit production dispatch until reviewed proof identifies the minimal change.

Read AGENTS.md, Git facts, this packet's C1–C5, `AgentMessageService.ts`, shared agent-messaging types/validation/storage, SessionManager messageAgent integration, createSession/sendMessage/respondToPermission/cancelProcessing, and RPC session ownership checks. Baseline is packet source manifest; refresh changes before acting.

1. Resolve exact existing host service initialization and real manager parent creation API. Write signatures and permission/context ownership in the source map.
2. Create proposed `packages/server-core/src/voice-tasks/admission-proof.test.ts` using real temporary receipt storage and injected fake execution; do not mock global fs or the entire package. Trace reservation, key digest, receipt correlation and child identity.
3. Prove stable intent recovery after launch accepted → reply lost → hangup/new call → retry. Verify preapproved/allow-all worker policy cannot bypass the voice-origin action cap. Prove duplicate concurrent launch returns one child; same key/different payload conflicts; foreign workspace fails; failed persistence cannot start a job; uncertain launched state cannot be blindly retried.
4. Cut execution at every boundary described in roadmap. Demonstrate recovery finding the receipt by persisted correlation. If existing receipt has no correlation hook, specify and fixture the narrow backward-compatible extension.
5. Model question continuation and cancelled/resolved sendMessage race explicitly. Record which production changes are needed; no assertion that a Promise resolution necessarily means task success.
6. Update C1–C5 with exact chosen signatures and migration/rollback details. Material contract changes increment revision and invalidate dependent review.

Validation from worktree root: `PANGOCAIRO_BACKEND=fontconfig bun test packages/server-core/src/voice-tasks/admission-proof.test.ts --path-ignore-patterns='**/release-artist-os/**' --path-ignore-patterns='**/dist/**'`. Also run existing AgentMessageService tests with the same ignore flags. Confirm discovered tests >0; use normal `.test.ts` fixtures, no package-wide mocks. Expected: all listed positive, denial and crash-cut assertions pass with one fake executor invocation at most per key.

Record command, exit code, test count, source revision/diff digest and fake/live boundaries in `evidence/T-101.md`. Stop if parent ownership or deterministic dispatch correlation cannot be specified without redesigning the agent engine. Return scoped extension choices, not a replacement runtime. Rollback deletes only this prototype's owned files; keep evidence and never touch real workspace receipts.
