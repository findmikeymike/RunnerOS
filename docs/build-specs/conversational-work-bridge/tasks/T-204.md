# T204 — Pre-live task details and explicit cancellation

r3; P2; authorized independent offline work before T203. Depends T201/T202 accepted implementation contracts. Own renderer task details component, call controller, useArtistManagerVoice hook and relevant tests only. Preserve SDK/host behavior. No app launch or providers.

Expose expandable current task details with full title, specialist, truthful state and saved outputs. Explicit Cancel task sends stable taskId/attemptId/requestId to existing host; duplicate presses and retry of uncertain request reuse identity. Do not optimistically claim cancelled, stop the call, cancel speech or replace work. Pending cancellation stays pending until a host terminal snapshot. A completed task keeps its result when cancellation races completion. Reject stale task attempts and detached call interactions. Keep keyboard controls and labels accessible and the collapsed call compact.

Test actual controller RPC identity/duplicates/stale attachment and host truth, component states/keyboard-native controls, affected voice regressions, Electron typecheck and Artist OS renderer build. Independent reviewer must inspect final changes and rerun decisive tests. Record evidence/T-204.md, final source/build hashes and explicit live-not-run status. T203 smoke covers the resulting candidate including real worker cancellation; do not accept later P3 requirements here.
