# T-102 — External assistant turn and playback proof

Revision r1; phase P1; owner implementer; reviewer independent-reviewer. Planned, executable after G-P1 and T-101. Requirements R-01/R-03/R-04/R-12/R-13/R-14. Own proof fixtures under proposed `apps/electron/src/renderer/lib/voice-task-delivery/` and the contracts document. Upstream production changes belong to T-202 after contract review.

Read C6–C7, current useArtistManagerVoice, focus transport, vendored VoiceCoreWeb JS/declarations, snapshot manifest, avatar/phoneme docs and `scripts/check-voice-core-snapshot.mjs`. Locate upstream `voice-core-rs/wrappers/web/src/VoiceCoreWeb.ts` under `/Users/michaelb.williams/Cascade Windsurf 3/Conversation AI System`; verify its Git identity before proposing an edit. Do not treat arbitrary upstream HEAD as the exact vendored build.

1. Map completeUserTranscript, pushAssistantText, generateAssistantResponse, audio queues, runtime-worker events and consumed-PCM viseme completion. Identify which signals actually prove foreground idle.
2. Freeze the minimum proposed event-turn API: explicit origin/turn ID, returned delivery handle, busy/deferred result, cancellation generation and playback completion. All changes backward compatible; event must not emit userSpeechComplete or trigger opener logic.
3. The system under test must include the actual VoiceCoreWeb implementation matching the vendored snapshot and its production runtime/audio event adapter. Fake only providers, clock, device and controlled PCM consumption at documented boundaries. Importing only a newly written scheduler proves its own design, not SDK compatibility. If the harness cannot exercise the real wrapper, record feasibility as unproven and do not pass G-P1-EXIT. Create proposed `apps/electron/src/renderer/lib/voice-task-delivery/playback-proof.test.ts` with fake clock/transport and deterministic PCM consumption. Test notification during generation, queued audio, silent gap, actual drain and new user speech.
4. Prove a single TTS and viseme path; interrupted completion is not marked delivered, old generations cannot restart audio, disconnect removes handlers, and one normal user response stays unaffected.
5. Document whether web-wrapper-only change suffices. If not, identify exact WASM contract need and return to review before P2. Do not patch vendored compiled JS as the authoritative source.

Validation from candidate root: `PANGOCAIRO_BACKEND=fontconfig bun test apps/electron/src/renderer/lib/voice-task-delivery/playback-proof.test.ts --path-ignore-patterns='**/release-artist-os/**' --path-ignore-patterns='**/dist/**'`. Record >0 discovered tests, all scenario outcomes, and inspected snapshot hash. Run `node scripts/check-voice-core-snapshot.mjs` as baseline integrity check; record failure rather than silently refreshing unrelated vendor files.

Evidence: `evidence/T-102.md` with diagram/signatures, tested mock boundaries, actual commands/results and missing live proofs. No microphone, provider calls, dependency download or app relaunch in this task. Rollback removes only proof files. Handoff freezes C6 and gives T-202 exact upstream files/tests and vendoring procedure.
