# T-301 — Interactive questions revisions and permissions

Revision r1; phase P3; owner implementer; independent reviewer required. Status planned; **outlined**. Expand into an executable packet after preceding gate acceptance, freezing exact implementation/test commands from P1 evidence. No permission to improvise unresolved interfaces.

Requirements: R-06, R-07, R-08. Dependencies: G-P3.

Owned resources: packages/server-core/src/voice-tasks, packages/server-core/src/sessions/SessionManager.ts, apps/electron/src/main/artist-manager-voice-focus.ts. Other agents may be working; preserve their changes and escalate overlapping ownership.

Consume contracts.md C1–C7 and relevant P1 frozen decisions. Deliver the named outcome using the existing host/session and audio paths. Preserve the specification invariants; no alternate voice runtime or new paid generation.

Validation: perform the corresponding phase scenarios in roadmap.md, including negative/recovery cases for every linked requirement. T-203 must prove a real child, saved draft, concurrent exchange and audible announcement. T-401 must run repository test/typecheck/build, SDK integrity, paired latency trials, repeated calls and disabled-feature rollback. For other tasks add scoped deterministic tests before phase-level live scenarios; mocks do not count as live acceptance.

Return evidence/T-301.md with exact build/diff identity, commands, observed outcomes, provider versus fixture boundaries, remaining gaps and reviewer verdict. Stop on contract change, missing authorization or unresolved ownership; do not mark accepted merely because code exists. Roll back only owned changes and retain task/output records needed for recovery.
