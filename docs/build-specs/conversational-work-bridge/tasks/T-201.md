# T-201 — Native task bridge

Revision r2; P2; owner implementer; independent reviewer required. Executable after G-P2. Requirements R-02/05/07/08/11/12. Implementation authorized 2026-09-12; no relaunch/commit/merge/push.

## Owned paths

packages/server-core/src/voice-tasks, packages/server-core/src/agent-messaging, packages/shared/src/agent-messaging, SessionManager's bounded dispatch/outcome/cap integration, shared session/launch types and shared backend pre-tool voice grant propagation, apps/electron/src/main/artist-manager-voice-focus.ts and minimal existing main/preload/shared/RPC plumbing needed to expose native bridge. Preserve concurrent work and existing uncommitted spec/proofs. No SDK/audio/hook changes in this task.

## Contract

Read T-101 evidence and r2 C1–C5. Implement native bridge using real manager parent/session, not focus ID. Default off, fail closed for unsupported runtime/action/capability. Do not copy the fixture as production; it deliberately omits full schema validation/recovery/permission integration.

1. Extract existing AgentMessageService wiring into a shared SessionManager host entry without weakening active-specialist/context/mode/source/skill validation. Resolve concierge parent through strict launch prep, persist/flush host binding, verify owner/workspace on every method. Recheck current focus owner against the authorized window workspace; no renderer paths or arbitrary parent/child IDs.
2. Persist schema-v1 intents/tasks atomically using workspace conventions. Stable clientRequestId and digest recover across call bindings; reserve before dispatch; same key changed scope conflicts. Persist host-only correlation on receipt before child creation and child metadata at creation. Workspace mutex covers admission and two-active cap. Restart quarantines uncertain work; no auto-resume/retry.
3. Add explicit execution-generation terminal outcomes from SessionManager processing stop. Do not infer success/cancel from resolved sendMessage. Preserve normal callers. Receipt-backed snapshots/events must be monotonic, sanitized, and owner/workspace scoped; do not claim saved output from prose alone.
4. Enforce immutable voice-origin scope in shared pre-tool path before trusted-tools/mode/remembered grants. Propagate into every supported backend and descendants. Sensitive/unknown operations cannot use standing allow-all or spoken yes. Existing exact action UI grants only, otherwise reject/fallback. Start narrowly with verified local read/draft output tools; unsafe shell/network paths must fail closed, never parse a command name as permission. Any backend missing enforcement must be unavailable for voice dispatch.
5. Cancellation targets stable task/attempt and then current generation; speech/hangup only detach subscriptions. Resolve terminal outcome honestly. Retain context distinctions and independent worker settings. Explicit host question state and continuation protocol can remain gated pending T-301, but unanswered question is never success.
6. Expose typed bind/reserve/lookup/launch/snapshot/subscribe/cancel/detach and output-reference validation through existing host routes. Keep capability off by default. Integrate focused voice work admission through typed tool intent only if provider capability is supported, never prose parsing. Admission failure retains explicit Command fallback; successful admission stays in the call. Persist factual task context separately from speech history.

## Validation

Run new service/storage/ownership/cap tests plus existing AgentMessageService tests with repository ignore flags. Test concurrent same-key launch, payload conflict, denied/foreign parent/child, failing persistence, every crash cut, allow-all/trusted/remembered bypass attempts, unsupported backend, cancellation races, two tasks, stale events, no fake saved artifacts. Run packages/server-core and apps/electron tsc --noEmit; affected shared pre-tool/backend tests. Use temporary workspace fixtures/injected providers, no real artist data or provider calls.

Record implementation hashes and exact commands/counts in evidence/T-201.md. Independent reviewer reruns decisive checks and audits actual permission and terminal paths. Accept only after meaningful findings fixed. Live worker/voice/output proof belongs to T-203. Rollback disables admission/delivery while preserving existing jobs/records and exposing their ordinary task/session fallback.
